What does FastTargetPred do ?
FastTargetPred allows to predict protein target(s) for query of one or multiple compounds.
Note: the query can include thousands of molecules.

How to install FastTargetPred ?
1. Download the folder
2. To launch FastTargetPred, you must use Python version 3.7 or superior.
   The help program includes every instruction to run the program. You can run it with the following command:
   python3.7 FastTargetPred.py -h
   

For more information, a manual is available (see FastTargetPred_manual.pdf).
